name: CI/CD Pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: |
          echo "========================================"
          echo "🚀 Hello from the AI Pipeline Showcase!"
          echo "🔍 Running basic system checks..."
          echo "✅ Everything looks good!"
          echo "========================================"
